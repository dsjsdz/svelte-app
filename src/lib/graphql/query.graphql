query CanIUse($input: String!) {
  canIUse(input: $input)
}

query Auth {
  auth
}

query Profile {
  profile {
    id
    email
    username
    profile_url
    status
    email_verified_at
    permissions
    role {
      id
      name
      display_name
    }
  }
}
